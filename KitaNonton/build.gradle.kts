// use an integer for version numbers
version = 10

cloudstream {
    language = "id"
    authors = listOf("Duro92", "CodeSanzz")
    status = 1 // 0: Down, 1: Ok, 2: Slow, 3: Beta only
    tvTypes = listOf(
        "AsianDrama",
        "TvSeries",
        "Movie",
    )

    iconUrl = "https://nontonfilm.gratis/favicon.ico"
}